import datetime
import math

class A:
  def __init__(self, capacity):
    self.cache = x()
    self.capacity = capacity

  def get(self, key):
    if key not in self.cache
        return -1
    else:
        return weight / math.log(self.now -  )
        
  def put(self, key, value, weight):
    self.cache[key] = value
    if len(self.cache) > self.capacity:
        self.cache.popitem
